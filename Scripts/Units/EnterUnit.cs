using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnterUnit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (!Global.insideUnit)
        {
            //Detect Mouse Click
            //Select Unit
            //Activate unit depending on the type - Camera, skills, abilities
        }
    }
}
